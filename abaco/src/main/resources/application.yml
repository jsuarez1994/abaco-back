spring:
  datasource:
    url: jdbc:mysql://localhost/abaco?useSSL=false&serverTimezone=Europe/Madrid
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    hibernate:
      ddl-auto: none
